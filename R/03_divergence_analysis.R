# 03: Divergence analysis
# Scores spread across specifications. Divergence = fragility signal.

source('R/utilities.R')
load_dependencies()
ensure_output_dir()

# models <- readRDS('output/models.rds')
# tidy each, bind, compute:
#   - range of point estimates
#   - sign stability
#   - significance consistency
#   - CV of estimate across specs
# write_csv(divergence, 'output/divergence_scores.csv')

cat("03_divergence_analysis.R: replace with your scoring code.\n")
